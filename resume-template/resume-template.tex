%-------------------------------------------------------------------------------
% CONFIGURATIONS
%-------------------------------------------------------------------------------
% A4 paper size by default, use 'letterpaper' for US letter
\documentclass[9pt, letterpaper]{article}

% Sets the length and width of the page
\usepackage[left=1cm,top=1cm,right=1cm,bottom=1cm,bindingoffset=0.5cm]{geometry} 
% Allows us to draw icons
\usepackage{fontawesome} 
% Allows hyperlinks
\usepackage{hyperref}
% Allows to modify line spacing
\usepackage{setspace}

% Allows to remove footer page number
\pagestyle{empty}

% For single spacing 
\singlespacing

\begin{document}

    %-------------------------------------------------------------------------------
    %	CONTENT
    %	Each file is imported seperately, it can be used in many resumes at once.
    %-------------------------------------------------------------------------------

    \input{cv/heading.tex}
    \input{cv/skills.tex}
    \input{cv/experience.tex}
    \input{cv/education.tex}
    \input{cv/projects.tex}
    % \input{cv/Competitions.tex}
    % \input{cv/certificates.tex}
    % \input{cv/achievements.tex}
    % \input{cv/languages.tex}

    %-------------------------------------------------------------------------------

\end{document}
